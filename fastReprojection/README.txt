This directory contains code for doing a sparse feature based reconstruction and re projecting the image quickly. It is used for measuring the scale of 2D parameters, such as floe size from the PSITRES images. Run testScript to run through the module on test data. 